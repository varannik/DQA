Add DataSentinel platform monorepo, AWS deploy tooling, and partner API docs

Implement the phased migration from the legacy backend into independently
deployable services: shared contracts, Platform API, three SQS workers,
CloudFormation infrastructure, and deployment scripts targeting eu-west-1.

Platform API (datasentinel-platform-api):
- FastAPI orchestration with S3 storage, SQS job dispatch, result worker
- Client-credentials auth, scoped JWTs, webhooks with HMAC signatures
- Feature flags for in-process vs distributed execution modes

Workers:
- DQA, correction, and AI engines as SQS consumers with shared contracts

Infrastructure (datasentinel-infra):
- Foundation stack: VPC, RDS PostgreSQL, S3, SQS+DLQs, ECR, ECS cluster
- Service stacks for platform-api and worker ECS services
- DataSentinelDeploy IAM policy and aws-preflight/cleanup helpers

Deploy and ops:
- make deploy-aws, preflight, cleanup-stack, export-openapi
- Fix service-stack parameter passing and build_push stdout capture
- Fix CloudFormation security group description (ASCII-only GroupDescription)
- Fix preflight CloudFormation validate-template probe template

Documentation:
- PLATFORM_MIGRATION_GUIDE.md execution plan
- Partner integration guide, AsciiDoc export, and checked-in OpenAPI 3.1 spec
  (also under ../docs/ — add to repo or commit separately if docs live outside)

Test plan:
- make test
- make preflight ENV=dev
- make export-openapi
- make deploy-aws ENV=dev (or SKIP_FOUNDATION=true for service stacks only)
