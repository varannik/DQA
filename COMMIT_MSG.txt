Fix idempotent DB migrations and add platform-api redeploy tooling

Make startup migrations safe to re-run on every ECS task launch and add
helpers for redeploying the Platform API without a full stack deploy.

Database migrations (datasentinel-platform-api):
- Track applied files in schema_migrations to skip re-execution
- Use engine.begin() for transactional migration batches
- Run migrations before admin/tenant seed in startup.py
- Remove demo project INSERT from init.sql (already created in startup.py)
- Improve startup retry logging and session cleanup

Deploy and docs:
- Add scripts/redeploy-platform-api.sh (ECR build/push, CFN update, ECS rollout)
- Add make deploy-admin-ui target delegating to datasentinel-admin-ui
- Link admin UI and integration docs from README

Test plan:
- Restart platform-api locally and confirm migrations log "Skipping" on second boot
- ./scripts/redeploy-platform-api.sh ENV=dev
- make deploy-admin-ui ENV=dev
